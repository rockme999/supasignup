# 번개가입 - 구현계획서 v1.0

**기준 문서**: PRD v1.2, 서비스계획서 v1.1, 기술스펙 v1.1
**작성일**: 2026-03-12
**예상 기간**: 7주 (2026-03-17 ~ 2026-05-02)

---

## 1주차: 프로젝트 셋업 + cafe24-client 포팅

### 구현 체크리스트

#### 1-1. 모노레포 초기화
- [x] npm workspaces 기반 모노레포 구조 생성
- [x] `tsconfig.base.json` 공통 TypeScript 설정
- [x] `packages/cafe24-client/` 패키지 초기화
- [x] `packages/bg-core/` 패키지 초기화
- [x] `workers/api/` Worker 프로젝트 초기화 (Hono)
- [x] `workers/widget/` Worker 프로젝트 초기화
- [ ] ESLint + Prettier 설정
- [x] `.gitignore` 설정

#### 1-2. Cloudflare 리소스 생성
- [x] Cloudflare 계정에 D1 데이터베이스 생성 (bg-dev: d420b951, bg-production: 254438d7)
- [x] KV 네임스페이스 생성 (bg-kv-dev: 6aaca4b2, bg-kv-prod: fe38c181)
- [x] `wrangler.toml` 설정 (D1/KV 바인딩 ID 반영 완료)
- [x] `wrangler dev` 로컬 개발 서버 동작 확인 (Miniflare 시뮬레이션)
- [x] bg.suparain.kr 도메인 → Workers Custom Domain 배포 완료 (Version: 74b50540)

#### 1-3. D1 스키마 적용
- [x] `schema.sql` 작성 (owners, shops, users, shop_users, subscriptions, login_stats)
- [x] shops 테이블: `allowed_redirect_uris`, `deleted_at` 필드 포함
- [x] users 테이블: `email_hash` 필드 포함, AES-GCM 암호화 주석
- [x] D1에 스키마 실행 (로컬 + 리모트 bg-dev 모두 완료, 6개 테이블 17 commands)
- [ ] 스키마 적용 확인 (테스트 데이터 INSERT/SELECT)

#### 1-4. bg-core 패키지
- [x] 공통 타입 정의 (`types.ts`): Shop, User, ShopUser, Owner, Provider 등
- [x] OAuth 프로바이더 설정 (`providers.ts`): PROVIDER_INFO (이름, 아이콘, 색상, URL)
- [x] 유틸리티 (`crypto.ts`): UUID 생성, AES-GCM 암호화/복호화, SHA-256 해시
- [x] PKCE 유틸리티: `generateCodeVerifier()`, `generateCodeChallenge()`

#### 1-5. cafe24-client 포팅
- [x] `Cafe24Client` 클래스 기본 구조 (fetch 기반)
- [x] `exchangeCode()` - Authorization Code → Access Token 교환
- [x] `refreshToken()` - 토큰 갱신
- [x] `apiGet()` / `apiPost()` / `apiPut()` / `apiDelete()` - Admin API 호출
- [x] `getStoreName()` - 쇼핑몰 이름 조회
- [x] `getCustomerByMemberId()` - 회원 조회
- [x] `createScriptTag()` / `deleteScriptTag()` / `listScriptTags()` - ScriptTag API
- [x] `verifyAppLaunchHmac()` - 앱 실행 HMAC 검증 (Web Crypto API)
- [x] `verifyWebhookHmac()` - 웹훅 HMAC 검증
- [x] 토큰 자동 갱신 콜백 패턴 구현

### 테스트 체크리스트 (1주차)

| ID | 테스트 | 유형 | 통과 |
|----|--------|------|------|
| T1-01 | cafe24-client: exchangeCode 정상 동작 (모킹) | 단위 | [x] |
| T1-02 | cafe24-client: refreshToken 정상 동작 | 단위 | [x] |
| T1-03 | cafe24-client: apiGet/Post/Put/Delete 정상 동작 | 단위 | [x] |
| T1-04 | cafe24-client: HMAC 검증 성공/실패 케이스 | 단위 | [x] |
| T1-05 | cafe24-client: ScriptTag CRUD 정상 동작 | 단위 | [ ] |
| T1-06 | cafe24-client: 토큰 만료 시 자동 갱신 | 단위 | [x] |
| T1-07 | cafe24-client: API 호출 실패 시 에러 처리 | 단위 | [x] |
| T1-08 | bg-core: AES-GCM 암호화/복호화 왕복 테스트 | 단위 | [x] |
| T1-09 | bg-core: SHA-256 해시 일관성 테스트 | 단위 | [x] |
| T1-10 | bg-core: PKCE code_verifier/code_challenge 생성 및 검증 | 단위 | [x] |
| T1-11 | D1 스키마: 모든 테이블 생성 확인 | 통합 | [x] |
| T1-12 | D1 스키마: 유니크 제약조건 동작 확인 | 통합 | [ ] |
| T1-13 | wrangler dev 로컬 서버 정상 기동 | 통합 | [x] |

---

## 2주차: OAuth Provider 핵심 구현

### 구현 체크리스트

#### 2-1. DB 쿼리 함수 (`db/queries.ts`)
- [x] `getShopByClientId(clientId)` - client_id로 shop 조회
- [x] `getShopById(shopId)` - shop_id로 조회
- [x] `upsertUser(provider, userInfo)` - 사용자 upsert (암호화 적용)
- [x] `getShopUser(shopId, userId)` - 쇼핑몰-사용자 매핑 조회
- [x] `createShopUser(shopId, userId)` - 매핑 생성
- [x] `getMonthlySignupCount(shopId)` - 해당 월 신규 가입 수 조회
- [x] `recordStat(shopId, userId, provider, action)` - 통계 기록
- [x] `createShop(data)` - 쇼핑몰 생성 (client_id/client_secret 자동 생성)
- [x] `updateShop(shopId, data)` - 쇼핑몰 수정 (member_count 직접 수정 차단)
- [x] `softDeleteShop(shopId)` - soft delete (deleted_at 설정)

#### 2-2. OAuth Provider 엔드포인트 (routes/oauth.ts)
- [x] `GET /oauth/authorize` 구현
  - [x] client_id로 shop 조회
  - [x] redirect_uri 화이트리스트 검증 (H2)
  - [x] provider 활성화 여부 확인
  - [x] PKCE code_verifier/code_challenge 생성 (H3)
  - [x] cafe24_state / social_state 분리 저장 (M7)
  - [x] KV에 oauth_session 저장 (10분 TTL)
  - [x] KV에 pkce:{state} 저장 (10분 TTL)
  - [x] 소셜 OAuth URL로 즉시 리다이렉트

- [x] `POST /oauth/token` 구현
  - [x] client_id + client_secret 검증
  - [x] KV에서 auth_code 조회 + 삭제 (일회용)
  - [x] Access Token 생성 + KV 저장 (2시간 TTL)
  - [x] JSON 응답 (access_token, token_type, expires_in)

- [x] `GET /oauth/userinfo` 구현
  - [x] Bearer token에서 access_token 추출
  - [x] KV에서 user_id 조회
  - [x] D1에서 사용자 정보 조회 (복호화)
  - [x] 카페24 SSO 표준 형식으로 반환

#### 2-3. OAuth 콜백 (routes/oauth.ts 내 /callback/:provider)
- [x] `GET /oauth/callback/:provider` 공통 구현
  - [x] 소셜 authorization code 수신
  - [x] KV에서 PKCE code_verifier 복원 + 삭제
  - [x] code → access_token 교환 (PKCE code_verifier 포함)
  - [x] access_token으로 사용자 정보 조회
  - [x] KV에서 oauth_session 복원
  - [x] D1에 사용자 upsert (PII 암호화)
  - [x] shop_users 존재 여부로 가입/재로그인 구분 (H5)
  - [x] 신규 가입 시만 shop_users 생성
  - [x] Authorization Code 생성 + KV 저장
  - [x] 과금 체크 (매월 100명 한도, /authorize에서 사전 체크)
  - [x] 통계 기록 (signup/login 구분)
  - [x] cafe24_state로 카페24 redirect_uri에 리다이렉트

#### 2-4. 카페24 앱 설치 플로우 (routes/cafe24.ts)
- [x] `GET /api/cafe24/install` - 앱 설치 시작 (HMAC 검증 → OAuth 리다이렉트)
- [x] `GET /api/cafe24/callback` - OAuth 완료 (토큰 교환 → shop 등록 → ScriptTag 삽입)
- [x] `POST /api/cafe24/webhook` - 앱 삭제 웹훅 (soft delete)

### 테스트 체크리스트 (2주차)

| ID | 테스트 | 유형 | 통과 |
|----|--------|------|------|
| T2-01 | /oauth/authorize: 유효한 client_id로 정상 리다이렉트 | 통합 | [x] |
| T2-02 | /oauth/authorize: 무효한 client_id → 400 에러 | 통합 | [x] |
| T2-03 | /oauth/authorize: redirect_uri 화이트리스트 미등록 → 400 에러 | 통합 | [x] |
| T2-04 | /oauth/authorize: 비활성 provider → 400 에러 | 통합 | [x] |
| T2-05 | /oauth/authorize: PKCE code_challenge가 소셜 OAuth URL에 포함 | 통합 | [x] |
| T2-06 | /oauth/authorize: cafe24_state ≠ social_state 확인 | 통합 | [x] |
| T2-07 | /oauth/callback: 정상 플로우 → auth_code 발급 + 리다이렉트 | 통합 | [ ] |
| T2-08 | /oauth/callback: 신규 가입 → shop_users 생성 + action=signup | 통합 | [ ] |
| T2-09 | /oauth/callback: 재로그인 → shop_users 미생성 + action=login | 통합 | [ ] |
| T2-10 | /oauth/callback: PII 암호화 저장 확인 (D1에서 평문 아님) | 통합 | [ ] |
| T2-11 | /oauth/callback: cafe24_state가 카페24 redirect에 포함 | 통합 | [ ] |
| T2-12 | /oauth/token: 유효한 auth_code → access_token 발급 | 통합 | [x] |
| T2-13 | /oauth/token: 동일 auth_code 2회 사용 → 실패 (일회용) | 통합 | [x] |
| T2-14 | /oauth/token: 무효한 client_secret → 401 에러 | 통합 | [x] |
| T2-15 | /oauth/userinfo: 유효한 access_token → 사용자 정보 반환 | 통합 | [x] |
| T2-16 | /oauth/userinfo: 만료된 access_token → 401 에러 | 통합 | [x] |
| T2-17 | DB queries: upsertUser 암호화 + email_hash 정상 | 단위 | [x] |
| T2-18 | DB queries: getMonthlySignupCount 월별 카운트 정확 | 단위 | [x] |
| T2-19 | DB queries: softDeleteShop deleted_at 설정 확인 | 단위 | [x] |
| T2-20 | 카페24 install: HMAC 검증 성공 → OAuth 리다이렉트 | 통합 | [x] |
| T2-21 | 카페24 callback: 토큰 교환 → shop 등록 → ScriptTag 삽입 | 통합 | [x] |
| T2-22 | 카페24 webhook: 앱 삭제 웹훅 → soft delete | 통합 | [x] |
| T2-23 | 카페24 webhook: 서명 없는 요청 → 401 | 통합 | [x] |
| T2-24 | 카페24 install: 만료된 timestamp → 400 | 통합 | [x] |
| T2-25 | 소셜 OAuth URL: Google 정상 빌드 (PKCE 포함) | 단위 | [x] |
| T2-26 | 소셜 OAuth URL: Kakao 정상 빌드 | 단위 | [x] |
| T2-27 | 소셜 OAuth URL: Naver 정상 빌드 | 단위 | [x] |
| T2-28 | 소셜 OAuth URL: Apple 정상 빌드 (form_post) | 단위 | [x] |
| T2-29 | /oauth/authorize: free plan 한도 초과 → 403 | 통합 | [x] |

---

## 3주차: 소셜 OAuth 프로바이더 연동

### 구현 체크리스트

#### 3-1. OAuthProvider 공통 인터페이스
- [x] `OAuthProvider` 인터페이스 구현 (`services/social.ts`)
- [x] `buildAuthUrl()` 공통 로직 (PKCE 파라미터 포함)
- [x] `exchangeCode()` 공통 로직 (code_verifier 포함)
- [x] `getUserInfo()` 공통 응답 매핑

#### 3-2. Google 프로바이더
- [x] Google Cloud Console에서 OAuth 2.0 클라이언트 생성
- [x] Redirect URI 등록: `bg.suparain.kr/oauth/callback/google`
- [x] `buildAuthUrl()` - scope: `openid email profile`
- [x] `exchangeCode()` - `https://oauth2.googleapis.com/token`
- [x] `getUserInfo()` - `https://www.googleapis.com/oauth2/v2/userinfo`
- [x] 응답 매핑: `id → providerUid`, `email`, `name`, `picture → profileImage`

#### 3-3. Kakao 프로바이더
- [x] Kakao Developers에서 앱 생성 + 동의항목 설정 + 비즈 앱 전환 완료
- [x] Redirect URI 등록: `bg.suparain.kr/oauth/callback/kakao`
- [x] `buildAuthUrl()` - scope: `profile_nickname profile_image account_email`
- [x] `exchangeCode()` - `https://kauth.kakao.com/oauth/token`
- [x] `getUserInfo()` - `https://kapi.kakao.com/v2/user/me`
- [x] 응답 매핑: `id → providerUid`, `kakao_account.email`, `kakao_account.profile.nickname`

#### 3-4. Naver 프로바이더
- [x] Naver Developers에서 애플리케이션 등록
- [x] Redirect URI 등록: `bg.suparain.kr/oauth/callback/naver`
- [x] `buildAuthUrl()` - `https://nid.naver.com/oauth2.0/authorize`
- [x] `exchangeCode()` - `https://nid.naver.com/oauth2.0/token`
- [x] `getUserInfo()` - `https://openapi.naver.com/v1/nid/me`
- [x] 응답 매핑: `response.id → providerUid`, `response.email`, `response.name`

#### 3-5. Apple 프로바이더
- [x] Apple Developer에서 Service ID + Key 생성 (.p8 키 발급 완료)
- [x] Redirect URI 등록: `bg.suparain.kr/oauth/callback/apple`
- [ ] client_secret JWT 생성 (ES256, Web Crypto API) — TODO: Week 3
- [x] `buildAuthUrl()` - scope: `name email`, response_mode: `form_post`
- [x] `exchangeCode()` - `https://appleid.apple.com/auth/token` (JWT secret 생성 미완)
- [x] `getUserInfo()` - id_token JWT 디코딩 (sub, email 추출)
- [ ] 최초 인증 시 name 저장 (이후에는 전달 안 됨)

#### 3-6. Wrangler Secrets 등록
- [x] `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`
- [x] `KAKAO_CLIENT_ID`, `KAKAO_CLIENT_SECRET`
- [x] `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`
- [x] `APPLE_CLIENT_ID`, `APPLE_TEAM_ID`, `APPLE_KEY_ID`, `APPLE_PRIVATE_KEY`
- [x] `ENCRYPTION_KEY` (AES-GCM용)
- [x] `JWT_SECRET` (대시보드 인증용)
- [x] `CAFE24_CLIENT_ID`, `CAFE24_CLIENT_SECRET` (카페24 앱)

### 테스트 체크리스트 (3주차)

| ID | 테스트 | 유형 | 통과 |
|----|--------|------|------|
| T3-01 | Google: buildAuthUrl에 올바른 scope, redirect_uri, PKCE 포함 | 단위 | [ ] |
| T3-02 | Google: exchangeCode 정상 토큰 수신 (모킹) | 단위 | [ ] |
| T3-03 | Google: getUserInfo 응답 매핑 정확 | 단위 | [ ] |
| T3-04 | Kakao: buildAuthUrl 정상 | 단위 | [ ] |
| T3-05 | Kakao: exchangeCode + getUserInfo 정상 | 단위 | [ ] |
| T3-06 | Kakao: 중첩 응답 구조 매핑 (kakao_account.profile) | 단위 | [ ] |
| T3-07 | Naver: buildAuthUrl 정상 | 단위 | [ ] |
| T3-08 | Naver: exchangeCode + getUserInfo 정상 | 단위 | [ ] |
| T3-09 | Naver: response.response 중첩 구조 매핑 | 단위 | [ ] |
| T3-10 | Apple: ES256 JWT client_secret 생성 | 단위 | [ ] |
| T3-11 | Apple: exchangeCode + id_token 디코딩 | 단위 | [ ] |
| T3-12 | Apple: 최초 인증 시 name 저장, 재인증 시 name 미전달 처리 | 단위 | [ ] |
| T3-13 | 전체 플로우: Google 실제 OAuth → callback → token → userinfo | E2E | [ ] |
| T3-14 | 전체 플로우: Kakao 실제 OAuth (테스트 계정) | E2E | [ ] |
| T3-15 | 전체 플로우: Naver 실제 OAuth (테스트 계정) | E2E | [ ] |
| T3-16 | 전체 플로우: Apple 실제 OAuth (테스트 계정) | E2E | [ ] |
| T3-17 | 비활성 provider로 authorize 요청 시 400 | 통합 | [ ] |
| T3-18 | 소셜 서비스 에러 응답 시 적절한 에러 처리 | 단위 | [ ] |

---

## 4주차: 위젯 + 스마트 버튼

### 구현 체크리스트

#### 4-1. 위젯 API
- [x] `GET /api/widget/config` - 쇼핑몰 활성 프로바이더 목록 반환
  - [x] KV 캐시 적용 (`widget_config:{client_id}`, 5분 TTL)
  - [x] 과금 한도 초과 시 빈 providers 반환 (버튼 숨김)
- [x] CORS 설정: `origin: '*'`, `methods: ['GET']`

#### 4-2. 위젯 JS (buttons.ts → buttons.js)
- [x] BGWidget 클래스 구현
  - [x] `init()` - shopId 추출 (script src 또는 data-shop), localStorage 읽기
  - [x] `loadConfig()` - `/api/widget/config` 호출
  - [x] `render()` - createElement + textContent로 DOM 생성 (XSS 방어)
  - [x] `renderButton()` - 프로바이더별 버튼 생성 (강조 포함)
  - [x] `sortProviders()` - lastProvider를 최상단으로 정렬
  - [x] `startAuth()` - 클릭 → OAuth authorize URL로 이동
  - [x] `saveLastProvider()` - URL 파라미터에서 provider 읽어 localStorage 저장
  - [x] `findLoginPage()` - 카페24 로그인 페이지 자동 감지
- [x] PROVIDER_INFO 상수 (이름, 아이콘 SVG, 브랜드 색상)

#### 4-3. 위젯 CSS
- [x] 인라인 CSS 작성 (JS에 포함)
- [x] `.bg-widget` 기본 스타일
- [x] `.bg-btn` 버튼 기본 스타일
- [x] `.bg-btn-highlight` 강조 스타일 (파란 테두리, 배경, 볼드)
- [x] `.bg-btn-google/kakao/naver/apple` 프로바이더별 색상 (inline style)
- [x] `.bg-flash` / 번개마크 스타일
- [x] `.bg-powered` 하단 powered by 스타일
- [x] 반응형 처리 (모바일 대응, @media max-width:480px)

#### 4-4. 위젯 빌드
- [x] 위젯 JS를 문자열 상수로 API Worker에 인라인 포함
- [x] CSS를 JS에 인라인 포함 (동적 style 태그 삽입)
- [x] `/widget/buttons.js` 라우트에서 서빙
- [x] Content-Type, Cache-Control, CORS 헤더 설정

#### 4-5. localStorage 연동
- [x] OAuth 완료 후 redirect URL에 `bg_provider` 파라미터 추가 (callback에서)
- [x] 위젯 초기화 시 `bg_provider` 파라미터 감지 → localStorage 저장 → URL 정리
- [x] 시크릿/프라이빗 모드에서 localStorage 실패 시 graceful fallback (try/catch)

### 테스트 체크리스트 (4주차)

| ID | 테스트 | 유형 | 통과 |
|----|--------|------|------|
| T4-01 | /api/widget/config: 활성 providers 정상 반환 | 통합 | [x] |
| T4-02 | /api/widget/config: 과금 한도 초과 시 빈 배열 반환 | 통합 | [x] |
| T4-03 | /api/widget/config: KV 캐시 히트 확인 | 통합 | [x] |
| T4-04 | /api/widget/config: CORS 헤더 정상 | 통합 | [x] |
| T4-05 | 위젯: 로그인 페이지 자동 감지 (카페24 셀렉터) | 코드검증 | [x] |
| T4-06 | 위젯: 첫 방문 → 모든 버튼 동일 표시 | 단위 | [ ] |
| T4-07 | 위젯: 재방문 → lastProvider 버튼 강조 + 최상단 | 단위 | [ ] |
| T4-08 | 위젯: 버튼 클릭 → 올바른 authorize URL로 이동 | 단위 | [ ] |
| T4-09 | 위젯: saveLastProvider → localStorage 저장 + URL 정리 | 코드검증 | [x] |
| T4-10 | 위젯: innerHTML 미사용 확인 (XSS 방어) | 코드리뷰 | [x] |
| T4-11 | 위젯: localStorage 불가 시 에러 없이 동작 | 코드검증 | [x] |
| T4-12 | 위젯: 운영자가 선택한 provider만 표시 (비활성 제외) | 통합 | [x] |
| T4-13 | 위젯: 모바일 화면에서 레이아웃 정상 | 수동 | [ ] |
| T4-14 | 위젯: buttons.js 로드 → 렌더링 완료까지 200ms 이내 | 성능 | [ ] |
| T4-15 | 위젯: buttons.js 서빙 Content-Type 정상 | 통합 | [x] |
| T4-16 | 위젯: 4개 프로바이더 정의 포함 | 코드검증 | [x] |
| T4-17 | 위젯: 반응형 CSS 포함 | 코드검증 | [x] |
| T4-18 | /api/widget/config: missing client_id → 400 | 통합 | [x] |
| T4-19 | /api/widget/config: unknown client_id → 404 | 통합 | [x] |

---

## 5주차: 대시보드 (쇼핑몰 관리 + 프로바이더 선택)

### 구현 체크리스트

#### 5-1. 대시보드 인증
- [x] `POST /api/dashboard/auth/register` - 운영자 회원가입 (PBKDF2 해시, Workers 호환)
- [x] `POST /api/dashboard/auth/login` - 운영자 로그인 (JWT 발급, 쿠키)
  - [x] Rate limiting 미들웨어 적용 (IP별 5회/5분)
  - [x] KV `login_attempt:{ip}` 카운터
- [x] 인증 미들웨어 (`middleware/auth.ts`) - JWT 검증, 모든 /dashboard API 적용
- [x] SameSite=Strict 쿠키 설정

#### 5-2. 쇼핑몰 관리 API
- [x] `GET /api/dashboard/shops` - 운영자 소유 쇼핑몰 목록 (deleted_at IS NULL)
- [x] `POST /api/dashboard/shops` - 쇼핑몰 등록
  - [x] client_id / client_secret 자동 생성
  - [x] allowed_redirect_uris 초기 설정
  - [x] 기본 enabled_providers 설정
- [x] `GET /api/dashboard/shops/:id` - 쇼핑몰 상세 (소유권 확인, secret 마스킹)
- [x] `PUT /api/dashboard/shops/:id` - 쇼핑몰 수정 (보호 필드 직접 수정 차단)
- [x] `DELETE /api/dashboard/shops/:id` - soft delete (deleted_at 설정)
- [x] `PUT /api/dashboard/shops/:id/providers` - 프로바이더 활성/비활성
  - [x] 유효 provider 검증
  - [x] 최소 1개 이상 체크
  - [x] KV 위젯 캐시 무효화
- [x] `GET /api/dashboard/shops/:id/setup` - SSO 설정 정보 (Client ID/Secret/URL)
- [ ] `POST /api/dashboard/shops/:id/verify` - SSO 연동 확인 (6주차 E2E에서)

#### 5-3. 대시보드 프론트엔드 (SSR)
- [x] Hono + JSX SSR 설정 (tsconfig jsx: react-jsx, jsxImportSource: hono/jsx)
- [x] 레이아웃: 사이드바 + 메인 콘텐츠 (`views/layout.tsx`)
- [x] 로그인 페이지 (`/dashboard/login`)
- [x] 회원가입 페이지 (`/dashboard/register`)
- [x] 홈 페이지 (`/dashboard`) - 통계 요약 + 과금 현황
- [x] 쇼핑몰 목록 페이지 (`/dashboard/shops`)
- [x] 쇼핑몰 등록 페이지 (`/dashboard/shops/new`) - 플랫폼 선택 폼
- [x] 쇼핑몰 설정 페이지 (`/dashboard/shops/:id`) - 프로바이더 ON/OFF 토글 포함
- [x] SSO 설정 가이드 페이지 (`/dashboard/shops/:id/setup`)
  - [x] 4개 SSO 항목 등록 안내 (provider별 Authorize URL)
  - [x] Client ID/Secret 복사 버튼
  - [x] Account Linking 활성화 안내
  - [ ] 단계별 스크린샷 가이드 (배포 후 작성)
- [x] 계정 설정 페이지 (`/dashboard/settings`) - 비밀번호 변경 포함
- [x] 로그아웃 (`/dashboard/logout`) - 쿠키 삭제 + 리다이렉트

### 테스트 체크리스트 (5주차)

| ID | 테스트 | 유형 | 통과 |
|----|--------|------|------|
| T5-01 | 회원가입: 이메일 중복 시 에러 | 통합 | [x] |
| T5-02 | 로그인: 올바른 이메일/비밀번호 → JWT 발급 | 통합 | [x] |
| T5-03 | 로그인: 잘못된 비밀번호 → 401 에러 | 통합 | [x] |
| T5-04 | 로그인: 5회 실패 → 5분 잠금 (rate limiting) | 통합 | [ ] |
| T5-05 | 로그인: 잠금 해제 후 정상 로그인 | 통합 | [ ] |
| T5-06 | 인증 미들웨어: 유효한 JWT → 통과 | 통합 | [x] |
| T5-07 | 인증 미들웨어: 만료/무효 JWT → 401 | 통합 | [x] |
| T5-08 | 쇼핑몰 등록: client_id/secret 자동 생성 확인 | 통합 | [x] |
| T5-09 | 쇼핑몰 목록: 다른 운영자의 쇼핑몰 안 보임 | 통합 | [x] |
| T5-10 | 쇼핑몰 목록: 삭제된 쇼핑몰 안 보임 | 통합 | [x] |
| T5-11 | 쇼핑몰 수정: 보호 필드 직접 수정 차단 | 통합 | [x] |
| T5-12 | 쇼핑몰 삭제: soft delete (deleted_at 설정, 데이터 보존) | 통합 | [x] |
| T5-13 | 프로바이더 변경: 빈 배열 → 400 에러 | 통합 | [x] |
| T5-14 | 프로바이더 변경: 무효 provider → 400 에러 | 통합 | [x] |
| T5-15 | 프로바이더 변경 후 KV 캐시 무효화 확인 | 통합 | [x] |
| T5-16 | SSO 설정 API: SSO URL 올바르게 반환 | 통합 | [x] |
| T5-17 | 대시보드 CORS: same-origin만 허용 | 통합 | [ ] |
| T5-18 | PBKDF2 해시: 동일 비밀번호 다른 salt | 단위 | [x] |
| T5-19 | JWT: 생성/검증/만료/잘못된 서명 | 단위 | [x] |
| T5-20 | 회원가입: 짧은 비밀번호 → 400 | 통합 | [x] |
| T5-21 | 쇼핑몰 상세: client_secret 마스킹 확인 | 통합 | [x] |

---

## 6주차: 대시보드 (통계 + 과금) + 카페24 SSO 통합 테스트

### 구현 체크리스트

#### 6-1. 통계 API + UI
- [x] `GET /api/dashboard/stats` - 통합 통계
  - [x] 전체 가입 수, 오늘 가입 수, 이번 달 가입 수
  - [x] 소셜별 가입 비율 (Google/Kakao/Naver/Apple)
  - [ ] 플랫폼별 가입 수 (MVP: 카페24만) → Phase 2
  - [ ] 스마트 버튼 효과 (강조 버튼 클릭률) → Phase 2
- [x] `GET /api/dashboard/stats/:shop_id` - 쇼핑몰별 통계
  - [x] 일별 추이 데이터 (최근 30일)
  - [ ] 가입 전환율 (버튼 노출 → 가입 완료) → Phase 2
- [ ] 통계 대시보드 UI (차트) → 5-3 SSR 구현 후 진행
  - [ ] 소셜별 가입 비율 막대 차트
  - [ ] 일별 추이 꺾은선 차트
  - [ ] 필터: 쇼핑몰 / 플랫폼 / 기간

#### 6-2. 과금 시스템
- [x] `GET /api/dashboard/billing/status` - 과금 현황
  - [x] 쇼핑몰별 당월 신규 가입 수 / 100명 한도
  - [x] 현재 플랜 상태
  - [x] 유료 전환 안내 (80명 이상 needs_upgrade 플래그)
- [ ] `POST /api/dashboard/billing/upgrade` - 유료 전환 요청
  - [ ] 카페24 수동 인앱결제 API 연동 → Phase 2
- [x] 과금 체크 로직 (widget.ts 내 구현 완료)
  - [x] login_stats 해당 월 count 기반
  - [x] 무료 + 100명 초과 → 위젯 API에서 빈 providers 반환
- [ ] 유료 전환 유도 알림
  - [ ] 80명: 대시보드 배너 → 5-3 SSR 구현 후
  - [ ] 95명: (이메일 알림 - Phase 2)
  - [ ] 100명: 대시보드 강조 알림 + 버튼 비활성화 → 5-3 SSR 구현 후

#### 6-3. 카페24 SSO 통합 테스트
- [ ] 카페24 테스트 쇼핑몰 생성 (또는 기존 활용)
- [ ] 카페24 앱스토어에 개발용 앱 등록
- [ ] SSO 4개 항목 등록 (Google, Kakao, Naver, Apple)
  - [ ] Authorize URL: `bg.suparain.kr/oauth/authorize?provider={provider}`
  - [ ] Token URL: `bg.suparain.kr/oauth/token`
  - [ ] UserInfo URL: `bg.suparain.kr/oauth/userinfo`
- [ ] Account Linking 설정 활성화
- [ ] E2E 테스트 실행

### 테스트 체크리스트 (6주차)

| ID | 테스트 | 유형 | 통과 |
|----|--------|------|------|
| T6-01 | 통계 API: 전체 가입 수 정확 | 단위 | [x] |
| T6-02 | 통계 API: 소셜별 비율 데이터 정확 | 단위 | [x] |
| T6-03 | 통계 API: 일별 추이 데이터 정확 | 단위 | [x] |
| T6-04 | 통계 API: 쇼핑몰 소유권 확인 (404) | 단위 | [x] |
| T6-05 | 과금: 50명 → usage_percent 50% | 단위 | [x] |
| T6-06 | 과금: 100명 → is_over_limit true | 단위 | [x] |
| T6-07 | 과금: 유료 → limit/usage null | 단위 | [x] |
| T6-08 | 과금: 80명 이상 → needs_upgrade true | 단위 | [x] |
| T6-09 | 과금: 80명 도달 시 대시보드 배너 표시 | 수동 | [ ] |
| T6-10 | **SSO E2E: Google 가입** → 카페24 회원 생성 확인 | E2E | [ ] |
| T6-11 | **SSO E2E: Kakao 가입** → 카페24 회원 생성 확인 | E2E | [ ] |
| T6-12 | **SSO E2E: Naver 가입** → 카페24 회원 생성 확인 | E2E | [ ] |
| T6-13 | **SSO E2E: Apple 가입** → 카페24 회원 생성 확인 | E2E | [ ] |
| T6-14 | SSO E2E: 가입 후 로그인 세션 생성 확인 | E2E | [ ] |
| T6-15 | SSO E2E: 재로그인 시 기존 회원 인식 + 세션 생성 | E2E | [ ] |
| T6-16 | SSO E2E: 스마트 버튼 → 재방문 시 강조 표시 | E2E | [ ] |
| T6-17 | SSO E2E: 다른 소셜로 동일 이메일 가입 → Account Linking 동작 | E2E | [ ] |
| T6-18 | SSO E2E: 과금 한도 초과 → 버튼 안 보임 | E2E | [ ] |

---

## 7주차: 버그 수정 + 최적화 + 출시 준비

### 구현 체크리스트

#### 7-1. 버그 수정 + 안정화
- [ ] E2E 테스트에서 발견된 버그 수정
- [ ] 에러 로깅 정리 (Workers Analytics 활용)
- [ ] 예외 상황 처리 보강
  - [ ] 소셜 서비스 장애 시 에러 페이지
  - [ ] D1/KV 타임아웃 처리
  - [ ] 카페24 API 토큰 만료 시 자동 갱신

#### 7-2. 성능 최적화
- [ ] buttons.js 파일 크기 최소화 (minify + gzip)
- [ ] 위젯 API 응답 시간 측정 + 최적화
- [ ] D1 쿼리 성능 확인 (인덱스 활용)
- [ ] Workers CPU 사용량 확인 (Free Plan 10ms 제약)

#### 7-3. 보안 최종 점검
- [ ] redirect_uri 검증 정상 동작 확인
- [ ] PKCE 전체 플로우 동작 확인
- [ ] PII 암호화 저장 확인 (D1 직접 조회로 평문 아님 확인)
- [ ] Rate limiting 동작 확인
- [x] CORS 설정 확인 (위젯: *, 대시보드: same-origin) - index.ts에서 설정
- [x] XSS 방어 확인 (innerHTML 미사용) - 코드리뷰 T4-10
- [x] client_secret 마스킹 확인 (설정 페이지) - T5-21
- [x] PBKDF2 해시 동작 확인 (Workers CPU 제약 내, bcrypt 대신 PBKDF2 사용)

#### 7-4. 출시 준비
- [ ] 개인정보 처리방침 작성 + 배포
- [ ] SSO 설정 가이드 문서 완성 (스크린샷 포함)
- [ ] 카페24 앱스토어 등록 정보 준비
  - [ ] 앱 이름, 설명, 스크린샷
  - [ ] 심사 기준 확인
- [ ] 프로덕션 환경 배포 (`wrangler deploy`)
- [ ] 프로덕션 D1/KV 리소스 최종 확인
- [ ] 도메인 라우팅 최종 확인 (bg.suparain.kr)
- [ ] 모니터링 설정 (Workers Analytics 대시보드)

### 테스트 체크리스트 (7주차)

| ID | 테스트 | 유형 | 통과 |
|----|--------|------|------|
| T7-01 | 프로덕션 배포 후 /oauth/authorize 정상 응답 | 스모크 | [ ] |
| T7-02 | 프로덕션 배포 후 /api/widget/config 정상 응답 | 스모크 | [ ] |
| T7-03 | 프로덕션 배포 후 대시보드 로그인 정상 | 스모크 | [ ] |
| T7-04 | 프로덕션 Google SSO E2E 전체 플로우 | E2E | [ ] |
| T7-05 | 프로덕션 Kakao SSO E2E 전체 플로우 | E2E | [ ] |
| T7-06 | buttons.js 파일 크기 < 30KB (gzip) | 성능 | [ ] |
| T7-07 | 위젯 로드 → 렌더링 < 500ms (3G 환경) | 성능 | [ ] |
| T7-08 | Workers CPU 사용량 < 10ms (Free Plan 호환) | 성능 | [ ] |
| T7-09 | 보안: 잘못된 redirect_uri로 authorize 요청 → 차단 | 보안 | [ ] |
| T7-10 | 보안: 만료된 auth_code 사용 → 실패 | 보안 | [ ] |
| T7-11 | 보안: D1에서 직접 users 테이블 조회 → 암호화 확인 | 보안 | [ ] |
| T7-12 | 보안: 대시보드 로그인 brute force → 5회 잠금 | 보안 | [ ] |
| T7-13 | 개인정보 처리방침 페이지 접근 가능 | 수동 | [ ] |
| T7-14 | 카페24 앱스토어 심사 제출 | 수동 | [ ] |

---

## 전체 마일스톤 요약

| 주차 | 마일스톤 | 핵심 산출물 | 완료 |
|------|---------|-----------|------|
| 1주차 | 기반 구축 | 모노레포, cafe24-client, D1 스키마, bg-core | [~] |
| 2주차 | OAuth Provider | authorize/callback/token/userinfo, 앱 설치 플로우 | [x] |
| 3주차 | 소셜 연동 | Google/Kakao/Naver/Apple 4개 프로바이더 | [x] |
| 4주차 | 위젯 | buttons.js, 스마트 버튼, localStorage | [x] |
| 5주차 | 대시보드 (관리) | 운영자 인증, 쇼핑몰 CRUD, 프로바이더 관리, SSR 페이지 | [x] |
| 6주차 | 대시보드 (통계/과금) + SSO 테스트 | 통계, 과금, 카페24 E2E 검증 | [~] |
| 7주차 | 출시 준비 | 버그 수정, 보안 점검, 앱스토어 제출 | [ ] |

---

## 선행/병행 작업 (개발 기간과 독립)

| 작업 | 담당 | 마감 | 완료 |
|------|------|------|------|
| 소셜 OAuth 개발자 앱 등록 (Google/Kakao/Naver/Apple) | 운영 | 3주차 전 | [x] |
| 카페24 앱스토어 개발자 등록 + 앱 생성 | 운영 | 2주차 전 | [x] |
| 카페24 테스트 쇼핑몰 확보 | 운영 | 6주차 전 | [ ] |
| 개인정보 처리방침 작성 | 운영/법무 | 7주차 전 | [ ] |
| bg.suparain.kr 도메인 설정 | 운영 | 1주차 내 | [x] |
| Cloudflare 계정 유료 전환 ($5/월) 검토 | 운영 | 6주차 전 | [ ] |
| 카페24 automatic_payment="T" 문의 | 운영 | 수시 | [ ] |
